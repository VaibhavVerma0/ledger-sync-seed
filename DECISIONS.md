TRANSFER is decided by pairing, not by text. A transfer is only visible as two legs: a debit on one tracked account and a matching credit on another within ~10 minutes. The corpus punishes keyword matching — a ₹12,000 IMPS/P2A/RAHUL SHARMA debit has no counter-leg and is spending; an ₹18,000 NEFT INWARD SELF credit has no counter-leg and is income. Categorisation therefore runs over the full set after ingest, not per transaction.

MICRO matches the UPI token, not a UPI/ prefix. A ₹0.50 debit labelled UPI MANDATE VERIFY is micro; matching on UPI/ would miss it and give 44 instead of 45 on account 9075.

The ledger is short by one transaction, deliberately. 145 of 4821's 146 expected transactions are evidenced by messages. The 146th is a ₹7,500 debit on 29 Jul 2026 visible only as a break in the balance chain; no message reports it. NormalizedTxn requires at least one source message id, so an inferred transaction cannot enter ledger.json. It is reported in reconciliation.json instead, and summary.json spend for 4821 is 7,500.00 below the fixture as a direct result. Every other figure across all three accounts matches to the paisa.

Reconciliation uses true balances only. The credit card quotes Avl Limit, which in this corpus is a per-message snapshot rather than a running figure (it returns to 199,858.01 repeatedly), so including it would generate 19 false discrepancies. Only Avl Bal / BalAvl / Available Balance are chained.

V1__initial.sql could not run. It declares id IDENTITY PRIMARY KEY; H2 2.x removed the IDENTITY data type and build.gradle pins 2.2.224, so migrate failed on a clean checkout. Changed to BIGINT GENERATED BY DEFAULT AS IDENTITY. verify.sh never touches the database, so the seed's CI did not catch it.

Legacy seed rows are excluded from ingest. V2__seed.sql holds 15 production rows including duplicates and the bad water-can row. They are the subject of the Task 4 backfill, not part of the corpus ledger, so migrate/ingest skip V2 and migrate legacy loads it on demand.